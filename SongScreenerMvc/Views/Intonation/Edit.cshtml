@model SongScreenerMvc.Models.Intonation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Intonation</legend>

        @Html.HiddenFor(model => model.IntonationID)

        <div class="editor-label">
            @Html.LabelFor(model => model.IntonationName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IntonationName)
            @Html.ValidationMessageFor(model => model.IntonationName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
