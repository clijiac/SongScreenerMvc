@model IEnumerable<SongScreenerMvc.Models.Tone>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            ToneName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ToneName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ToneID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ToneID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ToneID })
        </td>
    </tr>
}

</table>
