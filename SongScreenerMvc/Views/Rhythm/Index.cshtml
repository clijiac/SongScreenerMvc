@model IEnumerable<SongScreenerMvc.Models.Rhythm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            RhythmName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RhythmName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RhythmID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RhythmID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RhythmID })
        </td>
    </tr>
}

</table>
