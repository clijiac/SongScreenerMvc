@model SongScreenerMvc.Models.Rhythm

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Rhythm</legend>

        @Html.HiddenFor(model => model.RhythmID)

        <div class="editor-label">
            @Html.LabelFor(model => model.RhythmName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.RhythmName)
            @Html.ValidationMessageFor(model => model.RhythmName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
